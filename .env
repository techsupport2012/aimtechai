# Server config
PORT=10500

# Emergency recovery token — bypasses normal login.
# Use POST /admin/emergency-login with { token } (and optionally
# { target_email } to pick a specific admin) to get a session
# in case of lockout. Logs in as an EXISTING admin — does not
# create or store a new user row, so leaves no trace in the CRM.
# Rotate this regularly. KEEP SECRET. Never commit this file.
ADMIN_RECOVERY_TOKEN=Maire_cai@072502R

# ===== MySQL (Hostinger) =====
MYSQL_HOST=srv2132.hstgr.io
MYSQL_PORT=3306
MYSQL_DATABASE=u122216429_aimtechai
MYSQL_USER=u122216429_aimtech_admin
MYSQL_PASSWORD=Yasiyas2011!

# Set to 'mysql' to use MySQL, 'sqlite' to keep using local SQLite (default)
DB_DRIVER=mysql
